## IDF Component Manager Manifest File
dependencies:
  ## Required IDF version
  idf:
    version: '>=4.1.0'
  # # Put list of dependencies here
  # # For components maintained by Espressif:
  # component: "~1.0.0"
  # # For 3rd party components:
  # username/component: ">=1.0.0,<2.0.0"
  # username2/component2:
  #   version: "~1.0.0"
  #   # For transient dependencies `public` flag can be set.
  #   # `public` flag doesn't have an effect dependencies of the `main` component.
  #   # All dependencies of `main` are public by default.
  #   public: true
  # # For local components:
  # espressif/usb_device_uac is vendored at components/usb_device_uac
  # (LOCAL EDIT: deeper EP-IN software FIFO); do not re-add the registry dep.
  # Its former transitive deps must be declared here directly - the component
  # manager does not resolve a vendored component's manifest:
  espressif/tinyusb: '^0.17.0~2'
  espressif/cmake_utilities: '*'
  espressif/button: =*
  joltwallet/littlefs: "==1.*"
